# 跨设备扫描模块（features/scan）所需的云端配置
# 复制本文件为 .env.local 并填入你的 Supabase 项目信息，然后重启 dev server。
#
# 获取方式：Supabase Dashboard -> Project Settings -> API Keys
#   Project URL  -> VITE_SUPABASE_URL
#   新版发布密钥（sb_publishable_…）或旧版 anon public -> VITE_SUPABASE_ANON_KEY
#
# supabase-js 2.116.0 同时支持新旧两种密钥格式，填哪个都行。
# anon / publishable key 是「公开客户端密钥」，设计上就是给前端用的，不是机密。
# 真正的访问控制靠的是数据库的 RLS 策略与存储桶策略，见 features/scan/docs/supabase-setup.sql。
#
# 首次接入还需要在 Supabase 的 SQL Editor 里执行一遍上面那个 SQL 文件，
# 否则：① 表不存在 ② 没有 Realtime 推送（电脑端收不到新资产）③ 没有存储桶。

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 手机端拍摄页的公网地址前缀（二维码内容）
#
# 本地开发时必须填，否则二维码会是 http://localhost:5173/scan —— 手机上的 localhost
# 指手机自己，必然 ERR_CONNECTION_REFUSED，这跟「是否同一 WiFi」完全无关。
# 填成已发布的公网站点后，手机用 4G/5G 也能打开拍摄页；两端连同一个 Supabase，
# 靠 Realtime 同步，所以不需要手机和电脑在同一局域网。
#
# 部署到公网后可以留空，会自动使用当前页面地址。
# 注意：不要带结尾斜杠。
VITE_SCAN_PUBLIC_ORIGIN=
